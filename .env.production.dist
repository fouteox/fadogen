# Example of public build configuration. CI uses .env.example directly.
# Supply runtime secrets through the deployment platform.

APP_NAME=Fadogen
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_HOST=fadogen.app
APP_URL="https://${APP_HOST}"

VITE_APP_NAME="${APP_NAME}"
